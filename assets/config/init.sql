CREATE TABLE Resource (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        name TEXT,
        value TEXT);

CREATE TABLE Password (
        id INTEGER PRIMARY KEY AUTOINCREMENT,
        title TEXT,
        content TEXT);